<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- background -->
    <item
        android:id="@android:id/background">
        <shape>
            <corners android:radius="86dp" />

            <gradient
                android:startColor="@color/colorPBGray"
                android:centerColor="@color/colorPBGray"
                android:centerY="1.0"
                android:endColor="@color/colorPBGray"
                android:angle="270"/>
        </shape>
    </item>
    <!-- progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="86dp" />

                <gradient
                    android:startColor="@color/colorPBGreen"
                    android:centerColor="@color/colorPBGreen"
                    android:centerY="1.0"
                    android:endColor="@color/colorPBGreen"
                    android:angle="270"/>
            </shape>
        </clip>
    </item>
</layer-list>